node_modules
dist
.env
.env.local
.env.*.local
*.db

# Runtime user uploads (seed-media/ is committed intentionally)
uploads/*
!uploads/.gitkeep

# Secrets — never commit
*-firebase-adminsdk-*.json
firebase-service-account.json
